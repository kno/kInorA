{
  "name": "kinora",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --success first -n web,api -c blue,green \"pnpm --filter web dev\" \"pnpm --filter api dev\"",
    "start": "node scripts/start-production.mjs",
    "start:api": "pnpm --filter api start",
    "start:web": "pnpm --filter web start",
    "build": "pnpm deps-guard && pnpm architecture && pnpm -r build",
    "type-check": "pnpm -r type-check",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r --if-present test:coverage",
    "test:e2e": "playwright test",
    "deps-guard": "node scripts/deps-guard.mjs",
    "architecture": "depcruise --config .dependency-cruiser.cjs packages/domain/src packages/contracts/src apps/api/src && node scripts/architecture-negative-test.mjs",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@vitest/coverage-v8": "3.2.4",
    "concurrently": "9.2.3",
    "dependency-cruiser": "^17.4.3",
    "typescript": "5.8.3",
    "vitest": "3.2.4"
  },
  "engines": {
    "node": ">=24.17.0"
  },
  "packageManager": "pnpm@10.17.1"
}
