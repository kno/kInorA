/**
 * Plan (scaffold) — protected page rendered inside the AppShell.
 *
 * Placeholder content until full plan management is implemented.
 */
export default function PlanPage() {
  return (
    <main className="kin-page">
      <div className="kin-card kin-card--center">
        <h1 className="kin-title">Plan</h1>
        <p className="kin-text kin-muted">
          Build and manage your personalized workout plans here.
        </p>
      </div>
    </main>
  );
}
